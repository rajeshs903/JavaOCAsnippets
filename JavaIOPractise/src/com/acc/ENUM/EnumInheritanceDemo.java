package com.acc.ENUM;

public class EnumInheritanceDemo 
{
	/*enum alphabets extends java.lang.ENUM
	{
		a,b,c,d
	}
	
	CE: Syntax error on token "extends", implements expected
	
	As every enum already extends java.lang.ENUM internally , we are not allowed to use Extends keyword explicitly
	but we can use "implements" 
	
	 
	*/

	public static void main(String[] args) 
	{
		

	}

}
